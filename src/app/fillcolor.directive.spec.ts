import { FillcolorDirective } from './fillcolor.directive';

describe('FillcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new FillcolorDirective();
    expect(directive).toBeTruthy();
  });
});
